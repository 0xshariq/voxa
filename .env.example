# Environment Configuration

# ===== HTTP Configuration =====
HTTP_TIMEOUT=5000
HTTP_BASE_URL=

# ===== Cache Configuration =====
# Caching is OPTIONAL - Voxa works perfectly without it
# Enable caching to improve performance for repeated requests

CACHE_ENABLED=false
CACHE_TTL=300000
CACHE_STORAGE=memory

# Storage options:
# - memory: In-memory cache (default, fast but not persistent)
# - file: File-based cache stored in ~/.voxa/cache/ (persistent across restarts)
# - custom: Use your own cache adapter

# Note: All cache data is stored locally in ~/.voxa/ directory
# No external dependencies or services required!

# ===== Queue Configuration =====
QUEUE_ENABLED=false
QUEUE_MAX_CONCURRENT=6

# ===== Retry Configuration =====
RETRY_COUNT=5
RETRY_DELAY=1000
RETRY_EXPONENTIAL_BACKOFF=true
